ls -la /hcaaapck/backup/DevEnvironment/SAS_Repository/CFX_STG | grep -i "PREMATCH"


find /hcaaapck/backup/DevEnvironment/SAS_Repository/CFX_STG -iname "*DTVNT*" -newer /tmp -mmin -60


find /hcaaapck/backup/SIT -iname "*DTVNT*PREMATCH*" -mmin -60 2>/dev/null
find /hcaaapck/backup/PROD -iname "*DTVNT*PREMATCH*" -mmin -60 2>/dev/null

find /hcaaapck/backup -iname "*DTVNT*PREMATCH*" -mmin -90 2>/dev/null



I checked /hcaaapck/backup/DevEnvironment/SAS_Repository/CFX_STG — the most recent files there are from April 9th/10th, nothing today. This confirms it's not where the reprocessed files are landing. Can you give me the exact, full destination path you're actually delivering to?
ls -la /hcaaapck/backup/DevEnvironment/SAS_Repository/CFX_STG 2>&1 | head -5


find /hcaaapck/backup -iname "*PREMATCH*" -mtime -1 2>/dev/null

find /hcaaapck/backup -iname "*ELV*" -mtime -1 2>/dev/null
find /hcaaapck/backup -iname "*DTVNT*" -mtime -1 2>/dev/null
$ find /hcaaapck/backup -iname "*DTVNT*" -mtime -1 2>/dev/null


ls -lat /hcaaapck/backup/DevEnvironment/SAS_Repository/CFX_STG | head -20

Hi Pranaya,

Quick update on the Pre-Match test file — the outbound file generation is confirmed working, but I'm still working through a CFX delivery/destination confirmation issue with the CFX team before I can hand you a verified test file.

Given today was the target for prod deployment, wanted to flag that this CFX piece may push that timeline. I'll follow up with the CFX team first thing tomorrow — let me know if there's any flexibility on timing, or if you'd want to explore an alternative path in the meantime.
