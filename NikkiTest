# from inside the correct repo (likely ncr_dna2_raa_files)
git checkout master
git pull origin master
git checkout -b feature/RBICA-14060

# copy the script into the repo, alongside merge_virtix_chase_files.sh
cp /path/to/virtix_chase_transform.py <repo>/path/where/merge_virtix_chase_files.sh/lives/

git add path/to/virtix_chase_transform.py
git commit -m "RBICA-14060: add _S 54-col to 22-col transform for Virtix chase files"
git push origin feature/RBICA-14060
