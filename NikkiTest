SELECT batch_id, recordid, add_val6
FROM U01_RAA.RAA_RAWZ.RAD_DEPLOY_ALL_LOB
WHERE recordid = '819216039'
  AND filedatetime = '20260514145424';

SELECT batchid, status, reccnt
FROM <riskadjustment.hive.table.loadlog>
WHERE processid = 4500 AND stepid = <stepId1>
ORDER BY batchid DESC LIMIT 1;
