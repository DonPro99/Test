adminpe-index.component.ts (frontend) — the isAutoPreApproval getter. Toggle ON is fixed; toggle OFF falls through to super.canAutoApprove(...).
pre-approval-base.component.ts (frontend) — canAutoApprove() method. Loops selected function codes, checks each has isAutomaticPreapproval === true. Logic itself looks correct.
CloaPreApprovalRequestViewModelMapper.cs (backend) — DeSerializePreApprovalFormData(). This is where RequestedAuthorizations gets its IsAutomaticPreapproval flag, sourced from a DB query: context.FunctionCodes.Where(x => x.DesigneeTypeId == model.DesigneeTypeId).
FunctionCodes table (DB) — suspected root cause. If rows where DesigneeTypeId = 31 (ADMINPEGEN) have IsAutomaticPreapproval = false/null while the equivalent Admin-PE codes have it true, that's the actual bug — a data/config gap, not application code.
