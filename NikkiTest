Issue #1 — Pre-Approval list "Pre-Approval Type" column not populating for Admin-PE-GEN

Files changed:

AfsPreApprovalRequestService.cs — in GetPreApprovalList():
Added 5 new fields to the first Select projection: IsAdminPeGenFPE, IsAdminPeGenMCE, IsAdminPeGenGIE, IsAdminPeGenFIRE, IsAdminPeGenBAE (reading from p.AfsPreApprovalRequest), plus a new field for the real designee type (regardless of IsInternal)
Added a new if (designeeTypeId == ADMINPEGEN) block inside the second data.Select(d => {...}), building the display string (e.g. "FPE, MCE") from the 5 booleans, before the existing "Other Activity" fallback
ActivityPaperWorkViewModel.cs — added matching properties:
csharp
   public bool IsAdminPeGenFPE { get; set; }
   public bool IsAdminPeGenMCE { get; set; }
   public bool IsAdminPeGenGIE { get; set; }
   public bool IsAdminPeGenFIRE { get; set; }
   public bool IsAdminPeGenBAE { get; set; }
   public int? [whatever you named the new designee type field] { get; set; }
Issue #2 — Post Activity Report showing "Make/Model/Series sought" for Admin-PE-GEN

Files changed:

adminpe-index.component.ts (the Post Activity version — under the post activity/afsgroups path, not the PreApproval one) — in ngOnInit():
Added one line right after super.initialize():
ts
   this.designeeTypeId = this.data?.preApprovalRequest?.designeeTypeId ?? DesigneeTypeEnum.ADMINPE;

This overrides the constructor's hardcoded DesigneeTypeEnum.ADMINPE with the real designee type from the actual record, before buildForm() uses it.

That's 3 files total across both issues. Want me to also prep a short written summary (like a ticket comment) combining both, or is this list enough for you to send as-is?
