does this current PROD script (with ${JDBC_VER} variables and both --packages AND --jars) actually work as-is, or does the leftover --jars reference to nonexistent local Snowflake jars still cause a failure?
