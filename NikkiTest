Right, the metadata already exists from the original setup — that's expected. The issue is that the existing metadata entry still includes the FILENAME column, which was recently dropped from the base table. Can you update that existing metadata entry to remove FILENAME? That should let the views regenerate without the 'invalid identifier' error I'm seeing in U01."
