"I previously submitted a metadata ticket for RAD_DATAVANT_CHASE_PREMATCH to set up tokenization metadata and create views (D01/T01/U01). Since then, one column (FILENAME) was dropped from the table. Can you update the existing metadata/view definition to remove FILENAME, so the views can regenerate correctly? I'm hitting 'invalid identifier' errors in U01 because the view still references the dropped column
