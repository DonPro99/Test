const cloaToggleOn = this.data?.designeeInfo?.isAutoPreApproval; // or wherever this field actually lives on your model
if (v || cloaToggleOn) {
  this.saveRequest(saveData)...
} else {
  this.confirmationService.confirm({ ... });
}
