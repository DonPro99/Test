One more detail: I compared the SQL file across the pipeline — preprod, ir, and master (prod) all already have the same version (ENCNTR_GUID-based joins, with edl_load_dtm/edl_run_id columns). Only develop still has the old FILE_ID-based version.
