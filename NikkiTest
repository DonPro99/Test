Basically trying to switch our Snowflake login over to the newer key-pair method instead of passwords, and make sure it still works fine across all the different vendor schemas that use this same script. The code's already done and sitting in UAT — just kept getting blocked because the UAT config file kept reverting back to prod settings. Fixed that again just now, so ready to actually test it once I hear back on which schemas to check."

Fixed the UAT properties env issue again (env=prod was hardcoded, blocking testing) - ready to test now for 14101.

Quick question before I start - which schemas should I actually be validating against? Want to make sure I'm testing the right ones rather than assuming it's all of them.
s
