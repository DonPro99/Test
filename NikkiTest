SELECT s.name AS SchemaName, t.name AS TableName
FROM sys.tables t
JOIN sys.schemas s ON t.schema_id = s.schema_id
WHERE t.name LIKE '%AuthorizedLocation%'
   OR t.name LIKE '%DesignationLocation%'
   OR t.name LIKE '%LocationChange%'
ORDER BY TableName;

-- If a table like AuthorizedLocation or DesignationLocationRequest exists, try this once we know its name:
SELECT * FROM [DMS_Mimic].[dbo].[AuthorizedLocation] WHERE Id = 148495;  -- adjust table name once found
