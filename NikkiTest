# 1. Make sure you have the latest base branch
git checkout develop
git pull origin develop

# 2. Cut a brand new branch from that up-to-date base
git checkout -b feature/RBICA-13935-jarfix-09012026-v2

# 3. Pull ONLY your three fixed files in from your old branch —
#    not the whole branch history, just these files' current content
git checkout feature/RBICA-13935-jarfix-09012026 -- bootstrap_key_uat.sh
git checkout feature/RBICA-13935-jarfix-09012026 -- bootstrap_key_prod.sh
git checkout feature/RBICA-13935-jarfix-09012026 -- rad_mcaid_chase_all_lob_s3_to_snflk_load.py

# 4. Commit just those, on top of current develop
git add bootstrap_key_uat.sh bootstrap_key_prod.sh rad_mcaid_chase_all_lob_s3_to_snflk_load.py
git commit -m "RBICA-13935: bootstrap jar fix + properties copy + indentation fix"
git push origin feature/RBICA-13935-jarfix-09012026-v2
