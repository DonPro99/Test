SELECT pa.Id AS PostActivityId, pa.FormData
FROM [DMS_NEW].[dbo].[PostActivity] pa
JOIN [DMS_NEW].[dbo].[PreApprovalRequest] pr ON pa.PreApprovalRequestId = pr.Id
JOIN [DMS_NEW].[dbo].[Application] a ON pr.ApplicationId = a.Id
WHERE a.DesigneeTypeId = 31  -- 31 = ADMINPEGEN
ORDER BY pa.Id DESC
