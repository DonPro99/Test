Quick note for anyone testing RBICA-14070 (Datavant Pre-Match) in UAT:

To trigger the pipeline, just place the deploy file here:
s3://crln-652798729258-ssm-uat-filetransfer/srcfiles/withouttrlfile/raa/medicare/na/deply_dtvntrtpm_mra_stg/<timestamp>/incr/

Once it lands there, everything runs automatically:

Job 780009123 (ingest) auto-triggers on file arrival
ETL scheduler chains it into Job 780009122 (all-LOB) → then Job 790009120 (outbound)

No need to manually trigger anything else. Final output lands in RAD_DATAVANT_CHASE_PREMATCH (filter EDL_LOB_CD = 'MRA').
