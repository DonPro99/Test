Console.WriteLine($"[DEBUG] base.IsAutoPreApproval called. Selected={string.Join(",", model.TestCheckInformation?.SelectedRequestAuthorizations?.Select(f => f.Id) ?? new List<int>())}");
    if (model.IsAfsType)
