-- Confirm what status 1 vs 5 actually represents
SELECT * FROM [DMS_Mimic].[dbo].[CloaStatus];

-- Check if this "new" AO is legitimate/active, compare to the old one
SELECT Id, UserName, EmailAddress, StatusId, CreatedDate
FROM [DMS_Mimic].[dbo].[User]
WHERE Id IN (108936, 212065);
