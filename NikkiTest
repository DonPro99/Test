-- Check what Application/ApplicationVersion actually store
SELECT TOP 5 * FROM [DMS_Mimic].[dbo].[Application];
SELECT TOP 5 * FROM [DMS_Mimic].[dbo].[ApplicationVersion];


SELECT a.Id, a.ActivityTypeId, a.StatusId, a.CloaId, a.NewCloaId, a.CreatedDate,
       av.* -- or whatever columns hold the name/number
FROM [DMS_Mimic].[dbo].[Activity] a
JOIN [DMS_Mimic].[dbo].[ApplicationVersion] av ON a.ApplicationVersionId = av.Id
-- then filter av by name/number once you know the right column

